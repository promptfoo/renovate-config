name: CI Success

on:
  pull_request:

jobs:
  ci-success:
    name: CI Success
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      checks: read
      statuses: read
    steps:
      - name: Wait for all PR checks
        uses: promptfoo/.github/.github/actions/ci-success@bbb49881762f0dd20a32dae08e244dc0a45fe968 # main
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          timeout-seconds: 300
